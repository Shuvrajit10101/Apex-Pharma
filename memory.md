# memory.md — Apex-Pharma Living Memory

> **Purpose:** this file fills the gap between sessions. It is the project's working memory — read it at the start of every session and **append to it after every task**. If it isn't written here, it will be forgotten.
>
> **How to maintain (every session, every task):**
> - After each completed task, add a dated bullet under **Session Log** (what changed · decisions · evidence · next step).
> - Log every **minor change/assumption** under **Change & Decision Log**.
> - Keep **Open Questions**, **Current Status**, and **Next Steps** current — edit them in place, don't just append.
> - Keep it tight; move stale detail into `plan.md` or archive older sessions at the bottom.

---

## Project snapshot

- **Project:** Apex-Pharma — offline-first Windows desktop pharmacy management system.
- **Client goal:** production build to ship to a real single retail pharmacy (India).
- **Context:** single store · offline desktop · India (GST, Drug License, Schedule H/H1).
- **Repo:** https://github.com/Shuvrajit10101/Apex-Pharma  *(owned by the GitHub Expert agent)*
- **Canonical plan:** `plan.md`  ·  **Rules:** `CLAUDE.md`  ·  **Team:** `agents.md`
- **Stack (locked):** .NET 10 (LTS) · C# · WPF (MVVM) · SQLite + EF Core · QuestPDF · xUnit · Git/GitHub · Inno Setup/MSIX.

## Current status

- **Phase:** **Phase 1 (Core MVP) — COMPLETE & merged. Phase 2 (Round out) — ✅ COMPLETE & merged** (a–g all shipped; awaiting owner review + a deferred IST-stamping '2h' follow-up; then Phase 3). Phase 1: 1(a) auth · 1(b) masters · nav-shell · 1(c) purchase/GRN · 1(d) billing · 1(e) invoice+settings · 1(f) reports · 1(g) backup/restore — plus .NET 10 (LTS) upgrade + NU1903 fix.
- **Owner decisions applied (2026-07-02):** Schedule-X = doctor+Rx now, strict register → Phase 2; Pharmacist RBAC unchanged; **repo made PUBLIC**; **working copy relocated out of OneDrive**.
- **Repo:** live on GitHub, **PUBLIC**; `main` @ `f3e5a66` (**Phase 2 merged #16–#23** + **post-review closure: #24 Schedule-X→pharmacist-only, #25 opening-float override-reason**; clean, synced with `origin/main`; a governance `docs` commit rides on top after each merge); **branch protection ENABLED** (PR required + `Build & test (Release)` check; no force-push/deletion; admin can override). CI green (.NET 10). **440 tests, no vulnerable packages.** *(remaining closure work: change (4) IST-stamping + optional cleanup.)* ⚠️ **Canonical working copy = `C:\dev\Apex-Pharma`** (open the project HERE — the OneDrive `Desktop\pharma` copy is a stale leftover, do NOT use).
- **Now:** 🤖 **AUTONOMOUS CLOSURE LOOP — RESUMED (2026-07-03); ULTRACODE on.** Owner directed **total closure** (no further owner action needed; **fix failures automatically**; follow CLAUDE.md strictly). Implementing the 3 signed-off changes sequentially — each design→implement→review→fix→PR→merge: ✅**(1) Schedule-X pharmacist-only — MERGED (PR #24, `main` @ `0379ee7`, 433 tests; plan.md §4 updated: Cashier barred from X)** → **(2) opening-float override-reason [starting on `feature/dayend-float-reason`]** → (4) IST-stamping (invoice-date/bill-number/expiry roll at IST)** → then (opt) deprecate the legacy purchase-return overload. GSTR-1 = no-op. `main` @ `a6595aa`+gov `70c393c`, 424 tests. ⚠️ **stop the running app (PID 6104) before builds.** **Left for owner only (external, non-blocking):** CA confirms Schedule-X legal field list + HSN/GST rates · run the restore drill · set the cloud-backup folder. Then Phase 3 (plan §15).
  - **▶️ Loop running.** `main` @ `f3e5a66` (code) + governance docs commit rides on top. **Closure changes:** (1) Schedule-X pharmacist-only ✅ **merged #24** · (2) opening-float override-reason ✅ **merged #25** · (4) IST-stamping ← **STARTING on `feature/ist-stamping`** · (opt) legacy purchase-return cleanup. **(4) scope:** make the billing `today` (bill-number daily sequence + batch-expiry check) → IST, the **printed invoice date** → IST, and the `StockAdjustment` history window → IST — reuse g1's `DayWindow`/`ITimeZoneProvider`/`FakeTimeZoneProvider`; **stored timestamps stay `UtcNow`** (only day-derivation + display go IST); no migration. **On (4): impl → review → auto-fix → GitHub Expert PR/CI/merge → governance → then loop-closure summary.** Left for owner only (external, non-blocking): CA confirms Schedule-X legal fields + HSN/GST · restore drill · cloud folder. Then Phase 3.
  - **⚠️ ON RESUME — do NOT re-implement.** Branch is fully committed at `bc8366f`; only `memory.md` (governance) is modified in the working tree and is intentionally uncommitted on the feature branch. Verify with `git -C C:/dev/Apex-Pharma log feature/party-ledgers --oneline -2` + `git status`.
  - **Then (review→merge):** **Code Review + Security review** (money logic → mandatory) on the branch diff → apply fixes → **GitHub Expert** push → PR (link plan §3/§6.1/§15) → `Build & test (Release)` check green → squash-merge → delete branch → update memory → proceed to **2(d) GSTR-1 export**.
- **Next (Phase 2) — ALL DONE ✅:** ✅(a) returns · ✅(b) adjustments/expiry · ✅(c) ledgers · ✅(d) GSTR-1 · ✅(e) day-end + Cashier · ✅(f) Schedule-X · ✅(g) IST day-window + barcode + restore runbook. **→ Phase-2 owner review (flags below) + deferred IST-stamping follow-up → then Phase 3 (plan §15).**

---

## Session Log

### 2026-07-01 — Session 1 (planning & governance)
- **Built the `/software` skill** (`~/.claude/skills/software/`) from the *Software Development: A Practical Approach!* textbook — the SDLC methodology backbone for this project. Verified (structure + functional smoke test passed).
- **Studied 3 reference reports** (Medical Store "Pharmiz", Hari-Om Medical Store, EM's Pharmacy) via a parallel workflow. Harvested modules, data models, and gaps. Key finding: all three lack batch/expiry tracking, GST, and Schedule-H compliance.
- **Confirmed client choices:** single retail pharmacy · offline-first desktop · India (GST/DL/Schedule H) · real production build to ship.
- **Wrote the canonical plan** → `plan.md` (18 sections: requirements, ER model, architecture, workflows, tests, roadmap, risks).
- **Established governance:** created `CLAUDE.md`, `agents.md`, `memory.md`; consolidated the plan into `plan.md` (removed the interim `PHARMACY-SOFTWARE-PLAN.md`).
- **Next step:** get the §17 client answers, then have the GitHub Expert scaffold the repo and DevOps set up CI (Phase 0).

### 2026-07-01 — Session 1 (cont.) — Phase 0 kickoff
- **Toolchain recon:** git ✅; **no .NET SDK** (only .NET 6 runtime) → need .NET 8 SDK; **gh CLI** not installed. winget/choco available.
- **Toolchain install:** winget installs of .NET 8 SDK + gh failed (exit 1602 — need admin/UAC, unavailable non-interactively). Switched to **no-admin user-local install**: Microsoft `dotnet-install.ps1` → `%LocalAppData%\Microsoft\dotnet`; gh portable zip → `%LocalAppData%\Programs\gh`; both added to **User PATH** + `DOTNET_ROOT` set. *(running)*
- **Scaffolded the .NET 8 solution** (build agent): Domain (17 entities + 5 enums), Data (EF Core SQLite DbContext + repositories + design-time factory), Application (service interfaces + **fully-implemented, tested `GstService`**), Desktop (WPF/MVVM shell), Tests (xUnit GST tests). 67 files → local `main` `eb76f36`. Nothing pushed; PDFs/bin/obj git-ignored.
- **Git identity:** machine git already `Shuvrajit1010 / dkphomechoudhury@gmail.com` (matches repo owner) — good for pushing later.
- **Next:** once SDK ready → `dotnet restore/build/test` to verify scaffold + GST tests; then `dotnet ef migrations add InitialCreate`; then (on your OK) gh auth + push.

### 2026-07-01 — Session 1 (cont.) — Phase 0 verified & closed
- **Toolchain installed** user-local: .NET SDK **8.0.422** at `%LocalAppData%\Microsoft\dotnet`, gh **2.95.0** at `%LocalAppData%\Programs\gh`. Both on User PATH + `DOTNET_ROOT` set. NOTE for future shells: `dotnet-ef` spawns `dotnet` from PATH — prepend `%LocalAppData%\Microsoft\dotnet` so it finds the SDK (the old Program Files .NET 6 has no SDK).
- **Build verified:** `dotnet build -c Release` → 0 warnings / 0 errors across all 5 projects (incl. WPF). `dotnet test` → **9/9 GST tests pass**.
- **Two build fixes:** (1) `App` base fully-qualified to `System.Windows.Application` (CS0118 clash with the `ApexPharma.Application` project namespace); (2) explicit `HasKey` for `AuditLog`/`SaleReturn`/`PurchaseReturn`/`StockAdjustment` (plan §7.2 PK names don't match EF convention).
- **EF migration:** `InitialCreate` generated and applied → valid SQLite schema (`apexpharma.db`, gitignored). Migration files committed.
- **Committed** local `main` `33ae93d` (fix + migration). Still nothing pushed to GitHub.

### 2026-07-01 — Session 1 (cont.) — Phase 1(a): Auth/RBAC + GitHub
- **Phase 1(a) implemented** (workflow: implement → security review): DI foundation (composition root in `App.xaml.cs`, `AddApexPharmaData` extension in the Data layer); **`AuthService`** — PBKDF2-SHA256, 100k iterations, 16-byte salt, self-describing hash, `FixedTimeEquals` verify, generic login failure (anti-enumeration); **RBAC** `Permission` enum + `HasPermission` matrix (plan §4); `DbInitializer` seeds 3 roles + one Owner (`admin` / `Admin@123`, change-on-first-login); `LoginWindow`/`LoginViewModel` gate the shell.
- **Security review = approve-with-nits;** all 5 findings fixed: unique `User.Username` index + duplicate guard (migration `AddUserUsernameUniqueIndex`), UTC timestamps, test-connection cleanup, role-resolution fail-safe (explicit name switch), DbContext DI moved into the Data layer.
- **Verified:** build 0/0; **56/56 tests pass**. Commit `f0c80a2` on `feature/auth-foundation` (off `main` `33ae93d`).
- **GitHub:** user authenticated `gh` (device flow) as **Shuvrajit10101** (`repo` scope). Repo confirmed **empty + private**.
- **Deviation to confirm:** Pharmacist currently has `ManageProducts` (catalog add/edit) but NOT `EditPrices` (Owner-only). Plan §4 only bars Pharmacist from prices/users/settings — defensible, but flag for client (one-flag reversal in `AuthService.HasPermission` + a test).

### 2026-07-01 — Session 1 (cont.) — Phase 1(b): Master Data
- **Implemented** (workflow: implement → review): CRUD services + validation for **Category, Manufacturer, Supplier, Product** (`MasterResult<T>` result pattern, DTO inputs, async over UnitOfWork); RBAC-gated (new **`ManageSuppliers`** permission for Owner+Pharmacist; catalog edits require `ManageProducts`); soft-delete `IsActive` added to Category/Manufacturer/Supplier; a permission-gated Masters window wired into `MainWindow`. Validation: India GST slabs {0,5,12,18,28}, unique names (NOCASE) + filtered-unique barcode, required FKs, 15-char GSTIN format, StateCode 01–37, non-negative reorder.
- **Migration** `AddMasterDataConstraints`: IsActive columns + UNIQUE NOCASE indexes (Category/Manufacturer name) + filtered-unique `Product.Barcode`.
- **Review = approve-with-nits;** fixes applied: per-session DI scope for the Masters window (was leaking a root-scoped DbContext), StateCode validation, guarded deactivation (block when active products reference the master). **The `ToLowerInvariant` nit was a FALSE POSITIVE** — those `ToLower()` calls are inside EF→SQLite queries where `ToLowerInvariant` has NO translation (21 tests failed); `ToLower()` maps to SQLite `lower()` (server-side, culture-independent). Kept `ToLower()` intentionally — **do not "fix" this.**
- **Verified:** build 0/0; **122/122 tests**. Commit `1a7dfab` → merged to `main` via **PR #6** (squash `a8e6da2`); CI green.

### 2026-07-01 — Session 1 (cont.) — UX: single-window navigation shell
- Owner tested the running app and flagged the separate-window Masters had **no "back"**. Built `feature/nav-shell`: `INavigationService` (DI singleton) swaps module views IN PLACE in `MainWindow` (ContentControl + DataTemplates); persistent left nav with active-item highlight; **Masters is now an embedded `MastersView` UserControl**; **placeholder views** for unbuilt modules; a landing view after login. DbContext lifetime: fresh DI **scope per module visit**, previous disposed (reviewed ✓, no leak).
- **Tests project retargeted** `net8.0` → `net8.0-windows` (+UseWPF) + Desktop project ref so nav logic is unit-testable; `InternalsVisibleTo("ApexPharma.Tests")` for a test-only resolver seam.
- **Review = approve-with-nits;** both fixed: navigation **re-entrancy guard** (monotonic token → last-click-wins) and **non-fatal activation** (failures show a status banner instead of crashing; global `DispatcherUnhandledException` handler logs to `%LocalAppData%\ApexPharma\error.log`).
- **Verified:** build 0/0; **136/136 tests**. Commits `b91b61c` + `4176938` → merged to `main` via **PR #7** (`ac99fef`); client §17 answers committed (`47095fa`). CI green.

### 2026-07-01 — Session 1 (cont.) — Phase 1(c): Purchase/GRN + Inventory
- **Purchase/GRN** (`PurchaseService`, ONE ACID transaction): record a supplier purchase; each line (product, batch no, expiry, qty, cost, MRP, GST) **upserts a `Batch`** (existing (product,batch) += qty, else new lot; `SalePrice` defaults to MRP) → stock-in; header GST via `GstService`. Expiry on/before today rejected; qty/price/GST-slab validated; **stock never negative**; whole purchase rolls back on any bad line. **Purchase returns** (over-return blocked). Gated on `DoPurchases`.
- **InventoryService** (read-only + FEFO + AdjustStock): stock grid with near-expiry(90d)/expired/low-stock flags; `SelectBatchFefoAsync` + `AdjustStockAsync` (transactional, non-negative) ready for billing.
- **UI:** Purchases + Inventory are now REAL nav-shell modules (per-visit scope); Inventory colour-codes expired/near-expiry/low-stock. Added `ISessionContext` (acting user → CreatedBy). `ViewStock` gates Inventory.
- **Migration** `AddBatchProductBatchNoIndex` (composite `Batch(ProductId,BatchNo)`).
- **Review = approve-with-nits;** fixed: **intra-purchase duplicate-batch merge** (same (product,batch) across lines → one lot via in-transaction dictionary), **AdjustStock tests** (commit+audit; below-zero rollback), single-read inventory.
- **Verified:** build 0/0; **171/171 tests**. Commits `5c08e7a` + `e7d5dae` on `feature/purchase-grn` (off `main` `47095fa`). Awaiting merge.

### 2026-07-01 — Session 1 (cont.) — .NET 10 (LTS) upgrade
- **Installed .NET 10 SDK `10.0.301`** user-local at `%LocalAppData%\Microsoft\dotnet` (alongside the historical 8.0.422; `global.json` now pins `10.0.0` + `rollForward: latestFeature` so `dotnet --version` reports 10.0.301). First background install failed (`dotnet.exe` file-lock during zip extract — a stray dotnet build-server held it); killed processes and re-ran clean. Updated `dotnet-ef` global tool 8.0.28 → **10.0.9**.
- **Retargeted all 4 csproj** to `net10.0` (Domain/Data/Application) and `net10.0-windows` (Desktop + Tests); fixed `net8.0` mentions in csproj comments; bumped EF Core Sqlite/Design + DI/DI.Abstractions packages to `10.0.*`. Test-only packages (xunit, runner, Test.Sdk) left as-is (build green).
- **CI:** `dotnet-version` 8.0.x → **10.0.x**; `actions/checkout` v4 → **v7**, `actions/setup-dotnet` v4 → **v5** (both run on Node 24, clearing the Node-20 deprecation); fixed the `net10.0-windows` comment.
- **EF migrations reset:** deleted the 4 timestamped migrations + snapshot (kept `Migrations/README.md`), regenerated a single **`InitialCreate` under EF Core 10** (`ProductVersion 10.0.9`). Verified it reproduces the FULL schema — all indexes were already in `OnModelCreating`, so nothing was lost: `IX_Users_Username` (unique), NOCASE-unique `Category`/`Manufacturer` names, filtered-unique `Product.Barcode`, `IX_Products_Name`, unique `Sale.BillNo`, `Batch.ExpiryDate`, composite `Batch(ProductId,BatchNo)`. Applied to a fresh `apexpharma.db`; cleared the stale runtime dev DB so the app recreates it.
- **Docs:** README (runtime/SDK/download link/verify string + refreshed migration section), CLAUDE.md tech-stack line, plan.md §8 row, memory Stack line, and `Migrations/README.md` all → **.NET 10**.
- **Verified on .NET 10:** `dotnet build -c Release` → **0 errors** (only NU1903 advisory warnings on the transitive `SQLitePCLRaw.lib.e_sqlite3` 2.1.11 — pre-existing, not introduced by this upgrade); `dotnet test -c Release` → **171/171 pass**. Only remaining `.NET 8` strings are truthful historical session-log lines above. Committed on `feature/net10-upgrade`.

### 2026-07-01 — Session 1 (cont.) — Phase 1(d): POS billing
- **BillingService** (`CreateSaleAsync`, ONE ACID transaction): FEFO dispense across batches (earliest-expiry, non-expired, spans lots; expired blocked; insufficient stock → whole sale rejected); per-line CGST/SGST via `GstService`; **unique sequential `BillNo`** via a transactional `Billing.NextBillNo` Setting counter (self-heals from MAX+1; UNIQUE index backstop); **Schedule H/H1/X** require doctor + Rx (X gated same as H1; stricter narcotic register → Phase 2); **khata** — Credit payment requires a customer and adds the total to `Customer.Balance`; batch stock decremented transactionally (never negative); whole sale rolls back on any failure.
- **Bill-level discount** re-apportioned across lines (largest-remainder, to the paise) with **GST recomputed on the net** — Σ(SaleItem.LineTotal) == Sale.Total, header Discount == Σ line discounts (GST-correct for India: point-of-sale discount reduces taxable value).
- **CustomerService** (basic CRUD for credit customers) + **Billing UI** module (real, replacing the placeholder): product add → FEFO line preview → live totals → payment mode → customer picker (req. for Credit) → Schedule-H prompt → Complete Sale. `DoBilling` gated.
- **Review:** pass 1 = changes_requested (HIGH: Schedule X ungated; MEDIUM: bill-discount didn't reconcile) → fixed → re-review **approve-with-nits**. Deferred NIT: on-screen preview GST isn't recomputed on the bill-discounted net (display-only; authoritative receipt from BillingService is correct) — fold into the invoice/UI slice.
- **Verified:** build 0/0; **203/203 tests** on .NET 10. Commits `89f68f4` + `006e5cb` on `feature/pos-billing`. Awaiting merge.

### 2026-07-01 — Session 1 (cont.) — Phase 1(e): GST invoice + Settings
- **SettingsService + Settings module** (Owner-only, `ManageSettings`): pharmacy profile in the `Setting` key/value store — name, address, **GSTIN**, **DL number** (Form 20/21), phone, invoice footer, NearExpiryDays, tax-rounding; typed `PharmacyProfile`; defaults seeded.
- **InvoiceService** (QuestPDF **2026.6.1**, MIT; `LicenseType.Community` set at startup + a test ModuleInitializer): a **GST-compliant 80mm thermal receipt** — pharmacy header (GSTIN/DL/phone), bill no+date, cashier, customer, line items, **CGST/SGST tax-summary by rate/HSN**, subtotal/discount/round-off/total, payment mode, **Schedule-H doctor+Rx note**. Split into a layout-agnostic `InvoiceModel` (unit-testable) + `ThermalReceiptDocument` renderer (A4/A5 easy to add later). Print/preview via `IReceiptPrinter` (writes PDF to `%LocalAppData%\ApexPharma\Receipts`, opens default viewer); **reprint by bill no**.
- Fixed the deferred 1(d) **preview-GST nit** (live preview apportions the bill discount + recomputes GST on the net, matching the receipt).
- **Review = approve-with-nits**: the one LOW (tax-summary taxable footing) proved a **false alarm** — footing was already correct (`SaleItem.Discount` already includes the apportioned bill share); clarified the comment + added a regression test locking `Σ Taxable == Subtotal`. NIT (preview largest-remainder ≤0.01) left as acceptable display-only.
- **Verified:** build 0/0; **227/227 tests** on .NET 10; no vulnerable packages. Commits `9fb9a3e` + `6ae9155` on `feature/invoice-settings`. Awaiting merge.

### 2026-07-01 — Session 1 (cont.) — Phase 1(f): Reports
- **ReportService** (read-only): **Sales report** (per-bill + summary) with **profit** = Σ(line net ex-GST − Batch.PurchasePrice×qty, using the exact FEFO lot dispensed); **Schedule H/H1/X register** (legal — date, bill, drug, batch, qty, patient, **doctor**, **Rx**); **GST/HSN summary** (taxable/CGST/SGST grouped by HSN+rate, foots to the sales totals); **low-stock** + **near-expiry/expired** (reuse InventoryService).
- **Reports UI** (real module, replaces placeholder): report-type + date-range picker + grid + summary; **export** — CSV (RFC-4180 + BOM) for all, **A4 PDF** (QuestPDF) for the Schedule-H register + GST/HSN summary. `ViewReports` gated (Owner + Pharmacist). File I/O via `IReportFileService`.
- **Review = APPROVE** (clean). Tracked followups: **IST/UTC report day-boundary** (reports filter UTC `BillDate` vs local date → evening sales can shift day; convert the window to UTC before filtering — do before go-live) and the Cashier day-end/own-sales view.
- **Verified:** build 0/0; **241/241 tests** on .NET 10. Commit `f036616` on `feature/reports`. Awaiting merge.

### 2026-07-02 — Session 1 (cont.) — Phase 1(g): Backup & restore  [FINAL Phase-1 slice]
- **BackupService**: consistent SQLite snapshot via **`VACUUM INTO`** (WAL-safe, ReadWrite source, Pooling=false); **AES-256-GCM** encryption; key = random 32-byte data-key **DPAPI-wrapped** (CurrentUser + app entropy; sealed blob in `Setting Backup.WrappedKey`, never plaintext) — optional **PBKDF2 passphrase** scheme too. Timestamped `.bak` to a **local folder + optional cloud-synced folder**; **auto-daily** at login (one/day) + **manual**; **retention** (default 30, prunes). **Restore**: decrypt→temp→validate (integrity_check + core tables)→atomic swap; live-DB restore staged + applied at next startup; a corrupt/wrong-key backup never touches live data. **Owner-only** (`Backup` permission). Backup panel in the Settings module.
- **Review = approve-with-nits;** all 4 fixed: **atomic first-use key minting** (single-flight — prevents an unrecoverable backup), **surfaced cloud-copy failures** (was silently swallowed → now a warning to the Owner), **WAL-safe snapshot** (ReadWrite source), **zeroed transient plaintext**.
- **Verified:** build 0/0; **264/264 tests** on .NET 10; no vulnerable packages. Commits `1e7e33e` + `c76bda0` on `feature/backup-restore`. Awaiting merge.
- **Note:** the prior session hit its limit mid-slice; the backup-fix agent was re-dispatched this session (2026-07-02) and completed cleanly (`c76bda0`). Followups (pre-go-live): manual restore drill on the counter PC; owner sets the cloud folder; DPAPI backups are machine/user-bound → use the passphrase scheme for off-site recovery on another PC.

### 2026-07-02 — Session 2 — Owner review, decisions, repo public, relocation, Phase 2 kickoff
- **Resumed** after the prior session hit its limit; completed the **1(g) backup fixes** (atomic key minting, surfaced cloud-copy failures, WAL-safe snapshot, zeroed plaintext) and merged 1(g) (**PR #15**) → **Phase 1 complete**.
- **Owner reviewed Phase 1** and answered the 4 open decisions (see log below): Schedule-X keep + strict-later, Pharmacist RBAC unchanged, **repo → public**, **relocate out of OneDrive**.
- **Repo made PUBLIC** after a clean secret-scan (full history; only the documented `Admin@123` bootstrap default). **Branch protection enabled** on `main` (PR + `Build & test (Release)` check, no force-push/deletion, enforce_admins=false). Governance commit `b3b644e`.
- **Relocated** working copy out of OneDrive → fresh clone at **`C:\dev\Apex-Pharma`** (main @ b3b644e; verified build 0/0 + 264 tests there). All future work + governance edits happen there.
- **Started Phase 2(a) Returns** (`feature/returns`).
- ⚠️ **Merge note:** `main` now enforces PR + the `Build & test (Release)` status check → Phase-2 merges wait for that check (admin can override if needed).

### 2026-07-02 — Phase 2(a): Sales & Purchase Returns
- **Sales return** (`SaleReturnService`, ONE ACID transaction across all lines): find sale by Bill No → return qty ≤ (sold − already-returned) per line → **restock the exact dispensed batch** → reverse CGST/SGST proportionally (fraction = returnQty/soldQty, AwayFromZero, consistent with GstService) → record `SaleReturn` (now carries `SaleItemId` + Cgst/Sgst) → **khata**: credit-sale return reduces `Customer.Balance` (capped — never drives an unrelated negative). Gated `DoBilling`.
- **Purchase return**: strict **per-line** destock (`ProcessPurchaseReturnLineAsync`), over-return blocked, never negative; the legacy batch-level overload is kept for a Phase-1c test (weaker cap, NOT used by the UI — flagged to deprecate).
- **Per-line return tracking:** added `SaleReturn.SaleItemId` + Cgst/Sgst and `PurchaseReturn.PurchaseItemId` → migration **`AddReturnTracking`**. Dedicated Sales-Return + Purchase-Return nav modules (removed the old first-line-only purchase-return panel).
- **Review = APPROVE** (clean). Nits: read-path lookup unguarded (fine — mutation is gated); `DoReturns` permission exists but this slice used `DoBilling`/`DoPurchases` per spec (logged); deprecate the legacy batch-level purchase-return overload in a later slice.
- **Verified:** build 0/0; **280/280 tests**. Commit `fa6fb6d` on `feature/returns`. Awaiting merge.

### 2026-07-02 — Phase 2(b): Stock adjustments & expiry write-off
- **StockAdjustmentService** (Application/Services/Inventory) — reuses `InventoryService.AdjustStockAsync` (one ACID tx, non-negative, writes a StockAdjustment audit row) for: manual **breakage/wastage** (delta + required reason), **physical-count correction** (set on-hand → records the delta), **expiry write-off** (single + bulk "write off all expired"; zeroes expired batches, reports value-lost at cost + MRP), and **adjustment history** query. `AdjustStock`-gated in the service (Cashier refused). New 3-tab Stock-Adjustments nav module. **No migration** (StockAdjustment + AdjustmentType already existed).
- **Review = APPROVE** (clean). 2 nits (acceptable, single-PC deployment): bulk write-off partial-failure reporting; count-correction reads current qty just outside the tx.
- **Verified:** build 0/0; **295/295 tests**. Commit `e962e82` on `feature/stock-adjustments`. Merged via **PR #17** (`89b52f0`); memory recorded in `c608ff1`.

### 2026-07-02 — Session 3 — bootstrap; verified 2(b) on `main`; starting 2(c) ledgers
- **Bootstrap** (CLAUDE→plan→memory→agents) in canonical `C:\dev\Apex-Pharma`. **Reconciled stale memory:** git confirms `main` @ `c608ff1`, clean, synced with `origin/main`; 2(a) (PR #16) + 2(b) (PR #17, `89b52f0`) both merged — corrected Current Status/Next Steps (were showing `3815deb` / "2(b) merging").
- **Starting Phase 2(c)** — supplier & customer ledgers/statements (plan §3 lightweight ledgers · §6.1 · §15 Phase 2). Dispatched an architect (Plan) agent to draft a concise ledger design — data model + migration + services + RBAC + UI + test list — before implementation.
- **Design approved (orchestrator):** adopted the architect's lightweight 2-entity design + all 5 decision defaults (logged below; no plan/§4 change; one assumption — supplier purchases treated as payables — flagged for owner review). **Dispatched the full-stack implementation agent** to build 2(c) on `feature/party-ledgers`: entities + `AddPartyLedger` migration + `ICustomerLedgerService`/`ISupplierLedgerService` + DTOs + DI + two nav modules + CSV/A4-PDF statement export + xUnit tests; verify build 0/0 + all tests green; commit locally.
- **Next:** Code + Security review → fix → GitHub Expert push/PR/merge (needs the `Build & test (Release)` check).
- **2(c) implementation COMPLETE (agent):** branch `feature/party-ledgers` @ `bc8366f`. New: `SupplierPayment`/`CustomerReceipt` entities · migration `AddPartyLedger` (2 tables, Restrict FKs, `(SupplierId,PaymentDate)`+`(CustomerId,ReceiptDate)` indexes, verified on a fresh DB) · `Customer/SupplierLedgerService` (+interfaces, `LedgerDtos`, `LedgerMath` carry-forward/running-balance) · `LedgerExporter` (RFC-4180+BOM CSV) + `PartyStatementDocument` (QuestPDF A4) · 2 nav modules (VMs+views, `ViewReports`-gated; write-actions gated `DoBilling` receipt / `DoPurchases` payment). Honored all decisions: over-payment blocked both sides · cash sales excluded (all-time closing reconciles to `Customer.Balance`) · supplier payable fully derived (no `Supplier.Balance`) · customer opening=0 (no field) · no new permissions · Purchase/Billing/SaleReturn services untouched. **One deviation:** `GetStatementAsync` takes `UserRole actingRole` (required for the mandated in-service `ViewReports` check). **Evidence:** build 0/0 · **328 tests pass** (was 295; +11 customer +10 supplier +3 exporter +9 RBAC) · no vulnerable packages. NOT pushed. **Reviewer follow-up:** owner-review flag — purchases treated fully on-account (no `Purchase.AmountPaid`; COD = purchase + matching payment).
- **⏸️ CHECKPOINT (usage limit, 2026-07-02):** paused BEFORE review/merge; nothing merged this session; `main` still @ `c608ff1`. **Resume recipe:** (1) confirm `feature/party-ledgers` @ `bc8366f` (do NOT re-implement); (2) Code Review + Security review (mandatory — money) on the branch diff; (3) apply fixes; (4) GitHub Expert push → PR (link plan §3/§6.1/§15) → CI `Build & test (Release)` green → squash-merge → delete branch; (5) update memory in place; then proceed to 2(d) GSTR-1 export.
- **↩️ RESUMED (Session 3, 2026-07-02, ultracode on):** verified `feature/party-ledgers` @ `bc8366f` intact (main still `c608ff1`; only `memory.md` dirty; diff = 31 files / ~4,029 insertions). Launched an exhaustive **7-lens adversarial review workflow** (money-math · tx-integrity · security/RBAC/compliance · plan/layering · test-adequacy · WPF-MVVM · reuse/efficiency), each finding independently re-verified by a skeptic before it counts.
- **Review results (20-agent workflow):** **13 findings, all verified (0 rejected) — 1 medium, 8 low, 4 nits; NO critical/high** (implementation fundamentally sound). **Fixing before merge (9):** (1) RBAC UI gate — CustomerLedger module was `ViewReports`-gated so a Cashier couldn't reach the khata-receipt screen though the service+test grant it via `DoBilling` → re-gate module on `DoBilling`, keep statement view+export `ViewReports`-only in the VM; (2) future-`InvoiceDate` guard in `PurchaseService.CreateAsync` (mirrors expiry check); (3-6) +4 test-hardening cases (exact-equal over-payment boundary · real post-mutation rollback · same-date tiebreak · date-window boundary); (7) DRY `ReportHeaderFactory` (BuildSubHeader triplicated); (8) clear stale rows on statement-fetch failure; (9) drop VM money arithmetic (re-fetch balance) + 2 client-side amount guards. **Deferred (logged):** UTC/local day-boundary → Phase-2(g) IST fix (pre-existing `ReportService.NormalizeRange` convention); ledger-VM base-class refactor (known cleanup); double-materialisation micro-opt (won't-fix). Dispatching one fix agent → re-verify build/tests → GitHub Expert PR/merge.
- **Review fixes applied → `218b034`:** all 9 fix-set items landed — RBAC module re-gated to `DoBilling` with `CanViewStatement`/`CanOpenCustomerLedger` flags + Cashier empty-state; `PurchaseService` future-`InvoiceDate` guard; +13 tests (exact-equal over-payment boundaries both sides, a real post-mutation rollback via new `ThrowOnSaveDbContext`, `LedgerMath` same-date tiebreak, UTC window-edge inclusivity, nav-permission cases); shared `ReportHeaderFactory`; stale-row clear on failed fetch; VM re-fetches authoritative balance; client-side amount guards. **build 0/0 · 341 tests · no vulns.** Deferred items untouched. *(Two fix-agent dispatches derailed into read-only recon first — no code impact; a third clean run landed the work.)* → GitHub Expert push/PR/CI/merge.
- **2(c) MERGED (PR #18):** `feature/party-ledgers` squash-merged to `main` → **`ea4e5ba`** (CI `Build & test (Release)` PASS 1m25s; branch deleted; 40 files, +4452/−21). Local `main` synced with `origin/main`. **Owner-review flags carried in the PR:** (1) cashiers may record khata receipts; (2) supplier purchases treated as fully on-account payables. **Phase 2: (a)(b)(c) done → next 2(d) GSTR-1 export.**

### 2026-07-02 — Session 3 (cont.) — Phase 2(d): GSTR-1 export
- **Governance commit** `ee0b634` (docs(memory) for 2(c)) pushed to `main` via admin bypass. **Design approved** for 2(d) (architect) + all 5 decisions defaulted (see Change & Decision Log): gross B2CS/HSN + separate returns section · POS=state-name · Docs-Issued in · readable multi-section CSV + A4 PDF · month/year picker. **Dispatched the full-stack implementation agent** on `feature/gstr1-export` — extend `ReportService`/`IReportService` + `ReportDtos` + `ReportExporter`/`Gstr1Document` + Reports-hub UI (`ReportType.Gstr1`) + xUnit tests; verify build 0/0 + tests green; commit locally.
- **2(d) implemented → `a3e2e2b`:** `GetGstr1Async(year,month,placeOfSupply)` + `Gstr1Report` (B2CS-by-rate · HSN+UQC/qty · Credit-Notes-by-rate SEPARATE · Docs-Issued · gross totals) reusing `GetHsnSummaryAsync`/`NormalizeRange` (reads stored CGST/SGST → reconciles to billed); multi-section CSV (`[b2cs]`/`[hsn]`/`[credit-notes]`/`[docs]`) + A4 `Gstr1Document`; Reports-hub month/year picker (`ReportType.Gstr1`, `ViewReports`-gated). **build 0/0 · 355 tests (+14) · no vulns.** Deviation: `Gstr1HsnRow.Description` reserved/empty (no HSN-description source). 11 files, +1150.
- **2(d) review (13-agent workflow):** **6 findings, all verified (0 rejected) — 1 medium, 3 low, 2 nits; NO critical/high** (production GST math correct — findings are test-hardening/quality). **Fixing (6):** (1) [med] add a fractional-GST divergence test so 'read stored, not re-derived' is falsifiable; (2) [low] add the missing `[b2cs]` CSV TOTAL footing row (+assert); (3) [low] assert gross empty in the returns-only-month test; (4) [low] make the docs-issued test order-sensitive (exercise the Ordinal bill-no sort); (5) [nit] extract shared `SumLine` helper (locks B2CS==HSN==HSN-summary reconciliation); (6) [nit] drop the dead UQC `OTH` double-fallback. Dispatching one fix agent → re-verify → PR.
- **Review fixes applied → `37fdc11`:** all 6 landed — [med] `Gstr1_..._NotAggregateReDerivation` test (2×5% lines, line-discount→net-25 → stored 1.26 vs aggregate 1.25, proven to go red on a re-derivation regression); `[b2cs]` CSV TOTAL row (+assert); returns-only-month gross-empty asserts; order-sensitive docs-issued test (forced out-of-order BillNos); shared `SumLine`/`LineFig` helper (locks B2CS==HSN==HSN-summary reconciliation); dead UQC fallback removed. **build 0/0 · 356 tests · no vulns.** → GitHub Expert push/PR/CI/merge.
- **Next:** GitHub Expert push/PR → CI → squash-merge → memory → 2(e).
- **2(d) MERGED (PR #19):** `feature/gstr1-export` squash-merged to `main` → **`618b3ec`** (CI `Build & test (Release)` PASS 1m30s; branch deleted; 11 files, +1285/−9). **Owner-review flag in the PR:** returns shown as a separate Credit-Notes section (net = gross − returns) + POS = state name. **Phase 2: (a)(b)(c)(d) done → next 2(e) day-end + Cashier view.**

### 2026-07-02 — Session 3 (cont.) — Phase 2(e): Day-end + Cashier view
- **Governance** `50e7128` (docs(memory) for 2(d)) pushed to `main`. **Design approved** via a **judge-panel workflow** (3 architects — lightweight-derive / auditable-entity / workflow-first → synthesis): derive-expected + persist a thin `DayEndClose` audit row (one per store-day, UNIQUE `BusinessDate`); whole-store Owner/Pharmacist close; **Cashier = read-only own-day view** (`CreatedBy`-scoped, server-coerced); reuse the `DayEnd` permission. All 5 decisions defaulted (Change & Decision Log).
- **Dispatched the full-stack implementation agent** on `feature/day-end` — `DayEndClose` entity + `AddDayEndClose` migration + `IDayEndService` (GetDaySummary / CloseDay ACID+server-recompute / GetCloseHistory) + DTOs + Day-End nav module (VM/View) + xUnit tests; verify build 0/0 + tests green; commit locally.
- **⚠️ Impl interrupted (account session limit) mid-work — NOT committed.** `feature/day-end` still @ `50e7128` with uncommitted WIP: `DayEndClose` entity + migration `20260702123455_AddDayEndClose` + DbContext + `Services/DayEnd/` + `ViewModels/DayEnd/` VM + nav enum/service + MainViewModel + DI + `DayEndServiceTests.cs`. Missing/unverified: `Views/DayEnd/DayEndView.xaml`(.cs), `MainWindow.xaml` nav button, `App.xaml` DataTemplate; build/tests not run; no commit. **Resuming with a finish agent** → complete UI wiring, build 0/0 + tests green + migration verify, then commit.
- **2(e) finished & verified → `26422ce`:** a finish agent completed the missing view (`DayEndView.xaml`+cs), `App.xaml` DataTemplate, `MainWindow` nav button, and the missing `AddTransient<DayEndViewModel>()` DI reg (the interrupted agent's service+entity+migration+VM+tests were already correct — no service corrections needed). 18 files, +3116. **build 0/0 · 375 tests (+19) · no vulns · migration UNIQUE `BusinessDate` verified.** → adversarial review.
- **2(e) review (15-agent workflow):** **9 findings, all verified (0 rejected) — 3 medium, 4 low, 2 nits; NO critical/high.** **Fixing (8):** [med] **honor the editable opening-float** — `CloseDayAsync` uses `input.OpeningFloat` for expected/variance/persist (was ignored; matches design decision #4) + VM live-recomputes Expected/Variance + fix DTO doc + test asserts honored; [med] **money-input validation** (`ValidatesOnExceptions` + gate close on parse errors + LostFocus — a stale/cleared CountedCash could otherwise close on an unintended figure & a stale 0.00 bypass the variance-note); [low] clean unique-race message (catch `DbUpdateException` → 'already closed' `Fail`, mirror `AuthService`) + race test; [low] closed-day cashier whole-store snapshot — fix docstrings (own-till = live path only) + test; [low] reframe the ACID-rollback test to its honest claim; [low] dedupe the double Sales query on the open path; [nit] assert stored `OpeningFloat` + second-close carry-forward. **Deferred:** [nit] day-window idiom duplication → Phase-2(g) shared day-boundary helper. Dispatching one fix agent → re-verify → PR.
- **Review fixes applied → `dcd75a4`:** all 8 landed — editable opening-float honored end-to-end (service persists `input.OpeningFloat`, VM live-recomputes; regression-guard verified) + money-input validation (`ValidatesOnExceptions`+LostFocus, close gated on `HasInputError`) + clean unique-race Fail (`DbUpdateException`→'already closed') + `DuplicateOnSaveDbContext` race test + closed-day cashier docs+test + reframed ACID test + deduped open-path Sales query + stored-`OpeningFloat` asserts. **build 0/0 · 377 tests (+2) · no vulns.** Deferred: day-window dedup → (g). → GitHub Expert PR/CI/merge.
- **2(e) MERGED (PR #20):** `feature/day-end` squash-merged to `main` → **`39bb6ca`** (CI `Build & test (Release)` PASS 1m44s; branch deleted; 19 files, +3404). **3 owner-review flags in the PR:** cashiers can't close the till (read-only own-day view) · SaleReturn cash-out uses the parent-sale proxy · opening float editable/overridable at close. **Phase 2: (a)–(e) done → next 2(f) Schedule-X register/dual-Rx.**

### 2026-07-02 — Session 3 (cont.) — Phase 2(f): Schedule-X register + dual-Rx
- **Governance** `648255f` (docs(memory) for 2(e)) pushed to `main`. **Design approved** (architect): separate `ScheduleXDispense` entity + dual-Rx capture enforced at billing for Schedule-X lines + a new derived running-balance register report. All decisions defaulted (Change & Decision Log); **kept §4 unchanged** — capture enforced for all billers, the pharmacist-only-for-X restriction flagged for owner (Rule #2). **Dispatched the full-stack implementation agent** on `feature/schedule-x` — `ScheduleXDispense` entity + `AddScheduleXDispense` migration + `BillingService` capture-enforcement + `GetScheduleXRegisterAsync` + exporter/`ScheduleXRegisterDocument` + billing + Reports UI + xUnit tests; verify build 0/0 + tests + migration; commit locally.
- **2(f) implemented → `6822e8b`:** `ScheduleXDispense` entity + migration `20260702183428_AddScheduleXDispense` (5 Restrict FKs + `(ProductId,DispensedAt)` index, verified) + `SaleInput.ScheduleX` + `BillingService` X-capture enforcement (in the sale ACID tx; **no §4 change** — capture under `DoBilling`; X sales backfill `Sale.DoctorName/PrescriptionRef` from prescriber/Rx via `??=` so they still feed the combined register) + derived `GetScheduleXRegisterAsync` running-balance + `ScheduleXRegisterDocument`/exporter + billing capture panel + Reports `ScheduleXRegister` UI. 23 files, +3028. **build 0/0 · 16 new tests green · no vulns.** ⚠️ **2 pre-existing ledger carry-forward tests FAIL locally** (`CustomerLedger`/`SupplierLedger` `...CarriesForward...`) = UTC-vs-local midnight-IST boundary flake, **proven pre-existing on pristine main**, green on UTC CI; the Phase-2(g) item, now manifesting (00:20 IST). Deviation: purchase 'received' leg dated by `Purchase.InvoiceDate` (matches supplier-ledger convention). → adversarial review (disregarding the 2 known flakes).
- **2(f) review (12-agent workflow):** **6 findings, all verified (0 rejected) — 2 medium, 1 low, 3 nits; NO critical/high.** **Fixing (3 groups):** [med×2 + nit] per-field capture-rejection `[Theory]` in `BillingServiceTests` (each required field blanked incl. `PrescriptionDate=default`, whitespace for some — only 1 of ~7 clauses was pinned = a compliance regression-resistance gap); [low] **`DispensedAt = sale.BillDate`** (was a 2nd `UtcNow` → a sale across UTC-midnight split the register Issued balance from its dispense-detail row; now reconciles) + equality test; [nit×2] make `XPrescriptionDate` nullable + client guard checks the date (a cleared DatePicker silently kept today's date). Dispatching one fix agent → re-verify → PR.
- **⏸️ PAUSED (user, 2026-07-03) mid-2(f):** nothing merged; `main` still `648255f`. A fix agent was mid-flight on `feature/schedule-x` applying the 3 review fixes (may land a commit atop `6822e8b`). **Resume recipe:** (1) verify `feature/schedule-x` state; (2) if the 3 fixes are committed & build 0/0 with ONLY the 2 named IST-flake tests failing → GitHub Expert push → PR (link §14/§9/§6.1; carry the owner flags: Schedule-X capture enforced for all billers / pharmacist-only-for-X deferred; register = derived running-balance) → CI(UTC green) → squash-merge → delete branch → governance commit → memory; else finish/re-apply the 3 fixes first; (3) then **2(g): IST day-boundary shared helper FIRST** (clears the 2 flakes), barcode, restore drill → Phase-2 owner review.
- **2(f) review fixes committed → `0e202ba`** (the paused fix agent completed): per-field capture `[Theory]` (`MissingXFieldMutators` — every required field incl. `PrescriptionDate=default`) · `DispensedAt = sale.BillDate` + equality test · nullable `XPrescriptionDate` + client-guard date check + `?? default` mapping. **build 0/0 · 396 pass / 2 fail (only the 2 named IST flakes) · Schedule-X 23/23 · no vulns.** 3 files, +69/−11. NOT pushed. **2(f) now READY TO MERGE on resume** (GitHub Expert PR → CI → squash-merge).
- **▶️ RESUMED (2026-07-03 10:09 IST) + 2(f) MERGED (PR #21):** past the IST-midnight window (the 2 flakes self-resolved). `feature/schedule-x` squash-merged to `main` → **`2230f21`** (CI `Build & test (Release)` PASS 1m50s; branch deleted; 23 files, +3086). **3 owner-review flags in the PR:** Schedule-X capture enforced for ALL billers / pharmacist-only-for-X deferred · register = derived running-balance · exact legal field set → advisor at go-live. **Phase 2 (a)–(f) DONE.** Per user request, launching the WPF app.
- **Next:** launch the app; then **2(g)** — IST day-boundary shared helper FIRST (`ReportService`/`LedgerMath`/`DayEndService`), barcode, restore drill → **Phase-2 owner review** (all accumulated flags).

### 2026-07-03 — Session 3 (cont.) — Phase 2(g): pre-go-live polish [LAST slice]
- **Started 2(g)** after 2(f) merged (PR #21) + app launched. Scope: (g1) **IST day-boundary shared helper** (FIRST — one helper mapping operator-facing local calendar days → UTC across `ReportService.NormalizeRange` + `LedgerMath` + `DayEndService` + GSTR-1/Schedule-X register windows; clears the 2 midnight-IST ledger-test flakes), (g2) **barcode polish** (billing scan-to-add), (g3) **restore-drill runbook** (doc; the physical drill is an owner go-live action). **Dispatched a design/recon agent** to scope g1+g2 concretely + the doc + decisions (timezone `Setting` vs IST-hardcode; barcode scope; 1 vs 2 PRs).
- **Design approved (architect):** **g1** = shared **`DayWindow`** helper (`Application/Time/`) mapping operator LOCAL calendar days → half-open UTC window via explicit `TimeZoneInfo`; TZ from a new **`Pharmacy.TimeZone` Setting (default 'India Standard Time')** via `ITimeZoneProvider`/`SettingsTimeZoneProvider` injected into `ReportService`/`Customer`+`SupplierLedgerService`/`DayEndService`; migrate every window call-site (`NormalizeRange`+sales/HSN/GSTR-1/Schedule-X, ledger statements, DayEnd cash-delta windows). **No stored-data/money-math change**; clears the 2 flakes. ⚠️ **keep `DayEndClose.BusinessDate` comparisons date-vs-date** (only UTC-shift `Sale.BillDate`-instant windows). **g2** = `IProductService.FindByBarcodeAsync` + billing scan box (Enter-to-add/refocus/unknown-error). **g3** = `docs/go-live-restore-drill.md`. **2 PRs (g1 first).** All 4 decisions defaulted (Change & Decision Log); deferred stamping items flagged for owner. **Dispatching the g1 implementation agent** on `feature/ist-day-window` (stops the running app before build).
- **g1 implemented → `5157bca`:** `DayWindow` helper + `ITimeZoneProvider`/`SettingsTimeZoneProvider` + `Pharmacy.TimeZone` setting + injected into ReportService/Customer+SupplierLedger/DayEnd; `LedgerMath.BuildStatement` gained `(fromUtc,toUtcExclusive,displayFrom,displayTo)` (filter UTC, display local); BusinessDate kept date-vs-date. 20 files, +654/−83. build 0/0 · 411 tests (locally, on IST host). No production/money-math change.
- **g1 review (10-agent workflow):** **4 findings, all verified (0 rejected) — 2 HIGH, 1 medium, 1 nit; 0 critical. PRODUCTION code correct; all findings = test-determinism + 1 stale comment.** ⚠️ **Real CI-flake risk:** ledger carry-forward + running-balance + `RecordPayment_WhenPersistFails` tests still build the window from `DateTime.Today` while rows stamp at `UtcNow` → after the IST-window shrink they drop today's rows on UTC/US hosts past 18:30Z (~23%+/day). **Fixing:** rewrite the affected `Customer`/`SupplierLedgerServiceTests` cases to FIXED dates + explicit `DateTimeKind.Utc` row instants (mirror the correct `...AtIstMidnightPlus5Min` tests; LEAVE `Statement_ExcludesCashSales` — doesn't flake) + fix the stale GSTR-1 window comment. No production change. Dispatching one fix agent → re-verify → PR.
- **g1 review-fixes applied → `6e898af`:** rewrote the 5 affected ledger tests (Customer/Supplier carry-forward + running-balance + `RecordPayment_WhenPersistFails`) to FIXED dates (`d=2026-06-15`) + explicit `DateTimeKind.Utc` row instants (mid-IST-day 06:00Z; pre-window −30d); left `Statement_ExcludesCashSales` + the correct boundary tests; fixed the stale GSTR-1 comment. **No production change** (only the ReportService comment + 2 test files). **build 0/0 · 411 tests · no vulns; a `TZ=UTC` run of the 28 ledger tests all passed → host-TZ/clock independent.** → GitHub Expert PR/CI/merge.
- **g1 MERGED (PR #22):** `feature/ist-day-window` squash-merged to `main` → **`c4f29ff`** (CI PASS 1m45s; branch deleted; 20 files, +723/−107). IST `DayWindow` helper live across reports/ledgers/day-end; the 2 midnight-IST flakes gone. **Only g2+g3 left in Phase 2.** **Dispatching g2+g3 implementation** on `feature/barcode-runbook`: `IProductService.FindByBarcodeAsync` + billing scan box (Enter-to-add/refocus/unknown-error) + `docs/go-live-restore-drill.md`. (Stop the app before build.)
- **g2+g3 implemented → `4257cf0`:** `IProductService.FindByBarcodeAsync` (exact/active/trim) + `BillingViewModel` scan (`BarcodeText`/`ScanBarcodeCommand`/`BarcodeAccepted`) sharing the extracted `AddProductLine` path with manual add + `BillingView` scan box (Enter-to-add) + code-behind refocus + `docs/go-live-restore-drill.md`. 8 files, +322. build 0/0 · 420 tests (+9) · no vulns.
- **g2+g3 review (8-agent workflow):** **4 findings, all verified (0 rejected) — 3 medium, 1 nit; 0 critical/high.** **Fixing:** [med×2, same root cause] scan-command **re-entrancy guard** — unguarded async fire-and-forget means a double-Enter (scanner CR+LF/auto-repeat) either double-adds a line OR throws a concurrent-operation exception on the shared scoped DbContext → add a `_scanInFlight` busy flag (early-return + `finally`) + clear `BarcodeText` before the await; [med] `ClearForm()` never cleared `BarcodeText` → leftover barcode leaks into the next customer's bill → clear it; [nit] add a scan-parity test (scanned line inherits FEFO preview + schedule flags via the shared `AddProductLine`). Dispatching one fix agent (stop app before build) → re-verify → PR.
- **g2+g3 review-fixes applied → `d019bc1`:** `_scanInFlight` re-entrancy guard (early-return + `finally`; clear `BarcodeText` before the await; not-found restores the code) — proven one-scan=one-line (`FindCallCount==1`, no throw) via a `GatedProductService` overlap test; `ClearForm` clears `BarcodeText`; +4 tests (overlap · reset-clears · FEFO-parity · Schedule-H parity). **build 0/0 · 424 tests · no vulns.** Only `BillingViewModel` + tests touched. → GitHub Expert PR/CI/merge.
- **🎉 g2+g3 MERGED (PR #23) — PHASE 2 COMPLETE:** `feature/barcode-runbook` squash-merged to `main` → **`a6595aa`** (CI PASS 1m17s; branch deleted; 8 files, +520/−9). **Phase 2 (Round out) fully shipped: (a)#16 (b)#17 (c)#18 (d)#19 (e)#20 (f)#21 (g1)#22 (g2/g3)#23.** main @ a6595aa · 424 tests · CI green · no vulns.
- **App relaunched** (main @ `a6595aa`, PID 6104, Login window) + **Phase-2 owner review presented**.
- **OWNER REVIEW ANSWERED (2026-07-03, via MCQ):** (1) **Schedule-X → REQUIRE a Pharmacist** (bar Cashiers; capture stays for Owner/Pharmacist) — now an **owner-approved §4 narrowing** → add a `DispenseScheduleX` gate in `BillingService` for X lines + `Permission`/`AuthService` + tests. (2) **Opening float editable, but an override REQUIRES a reason kept in a SEPARATE record** → new `DayEndClose.OpeningFloatReason` (migration) required when `OpeningFloat ≠ prior carry-forward`, persisted + shown in close-history. (3) **GSTR-1 credit-notes section = keep** (no change). (4) **IST-stamping follow-up → DO NOW** → bill-number/expiry rollover + printed invoice-date IST-aware (reuse `DayWindow`/`ITimeZoneProvider`). Un-actioned defaults remain: cashier khata-receipts · refund cash-out proxy · Pharmacist catalog RBAC · Schedule-X legal fields (CA).
- **Next:** await owner's instructions (order/bundling); confirm the (2) float-reason interpretation; then implement (1)+(2)+(4) each design→review→PR→merge → then Phase 3.

---

## Change & Decision Log
*(minor changes/recommendations applied — major changes need client sign-off)*

- **2026-07-01** — Renamed/consolidated the interim plan to the canonical `plan.md`. *(minor)*
- **2026-07-01** — Working product name **"PharmaDesk"** proposed in the plan; final name TBD by client (see Open Questions #8). *(assumption)*
- **2026-07-01** — Tech stack locked per `plan.md` §8 (.NET 8/WPF/SQLite). Change requires sign-off. *(decision)*
- **2026-07-01** — Toolchain installed **user-local (no admin)** because winget needed UAC; `.NET` at `%LocalAppData%\Microsoft\dotnet`, gh at `%LocalAppData%\Programs\gh`, both on User PATH. *(minor)*
- **2026-07-01** — Split returns into two entities `SaleReturn` + `PurchaseReturn` (plan §7.2 listed them combined); cleaner for EF. *(minor)*
- **2026-07-01** — `StockAdjustment` carries both `BatchId` and `ProductId` (batch integrity + product-level reporting). *(minor)*
- **2026-07-01** — Decimal precision via `[Column(TypeName="decimal(18,2)")]`; GST-rate columns `decimal(5,2)`. *(minor)*
- **2026-07-01** — RECOMMENDATION (open): move repo out of OneDrive (sync can lock `bin/obj`). Kept in place pending your call. *(recommendation)*
- **2026-07-01** — Auth: PBKDF2-SHA256 / 100k / 16-byte salt, self-describing hash; UTC for persisted timestamps. *(decision)*
- **2026-07-01** — DECISION TO CONFIRM: Pharmacist has `ManageProducts` (not `EditPrices`). Flip one flag if the client wants Pharmacist off the catalog. *(open)*
- **2026-07-01** — Runtime DB at `%LocalAppData%\ApexPharma\apexpharma.db` (off-repo); repo `apexpharma.db` stays gitignored / design-time only. *(decision)*
- **2026-07-01** — Added `ManageSuppliers` permission (Owner+Pharmacist) instead of overloading `ManageProducts`. *(minor)*
- **2026-07-01** — Added `IsActive` soft-delete to Category/Manufacturer/Supplier (Product already had it). *(minor)*
- **2026-07-01** — `ToLower()` (NOT `ToLowerInvariant()`) is INTENTIONAL in EF LINQ — maps to SQLite `lower()`, server-side + culture-independent; `ToLowerInvariant` has no EF-SQLite translation. **Do not "fix".** *(note)*
- **2026-07-01** — CLIENT ANSWERS (§17) resolved: 1 counter→SQLite; thermal-receipt-first; CSV/Excel importer; GST defaults-now; backup local+cloud; retail-only (Form 20/21); name **Apex-Pharma**. *(client sign-off)*
- **2026-07-01** — SCOPE (owner-approved): **credit-customer ledger (khata) is IN v1** (was optional/Phase-2). Build during the billing phase; `Customer.CreditLimit/Balance` already exist. *(major — owner signed off)*
- **2026-07-01** — Building a **single-window navigation shell** (`feature/nav-shell`) after the owner found the separate-window Masters had no "back"; placeholders for unbuilt modules. *(minor — UX fix)*
- **2026-07-01** — **STACK: upgraded .NET 8 → .NET 10 (LTS)** (owner-directed — .NET 8 EOL Nov 2026; .NET 10 LTS to Nov 2028). All projects/packages/CI/docs; EF migrations reset to one `InitialCreate` under EF Core 10. `plan.md §8` + `CLAUDE.md` updated. *(major — owner signed off)*
- **2026-07-01** — SECURITY: pinned `SQLitePCLRaw.bundle_e_sqlite3` → **3.0.3** (Data+Tests) to clear NU1903 / GHSA-2m69-gcr7-jv3q (transitive 2.1.11); `dotnet list package --vulnerable` clean. *(fix — issue #10)*
- **2026-07-01** — Also updated the `/software` skill's lone .NET-version mention to ".NET 10 (current LTS)". *(minor)*
- **2026-07-01** — GOVERNANCE (owner-directed): CLAUDE.md **Rule #1 → EXTREME agentic** — main session is a pure orchestrator (dispatch / synthesize / report / govern only); ALL product code, builds, tests, git/gh, and reviews delegated to agents/workflows. *(process change)*
- **2026-07-01** — MODE (owner-directed): complete **all remaining Phase 1 in one go, autonomously** (billing → invoice+settings → reports → backup), each internally implement→review→fix→merge; owner reviews the whole Phase 1 together at the end, no per-slice pauses. *(process)*

- **2026-07-02** — OWNER DECISIONS (post-Phase-1 review): (1) **Schedule X** stays gated like H1 (doctor+Rx) for v1; dedicated narcotic register / dual-Rx → **Phase 2**. (2) **Pharmacist RBAC** unchanged (manages products + suppliers; not prices/users/settings). (3) **Repo → PUBLIC** to enable free GitHub branch protection on `main` (owner accepted that the code becomes publicly visible; secret-scan first). (4) **Relocate repo out of OneDrive** → local working copy at `C:\dev\Apex-Pharma` (GitHub repo unchanged; future sessions open there). *(owner-directed)*
- **2026-07-03** — DECISIONS (2(g) polish): (1) TZ for day-windows = **configurable `Pharmacy.TimeZone` Setting, default 'India Standard Time'** (resolved via `TimeZoneInfo.FindSystemTimeZoneById`, fallback to hardcoded IST if unknown), NOT hardcoded; no Settings-UI field this phase. (2) **DEFER to a '2h' follow-up (flag at owner review):** `BillingService.cs:100` `UtcNow.Date` bill-number/expiry rollover at UTC-midnight + the 'display invoice dates in IST' question + `StockAdjustmentService.cs:213` history window — these are stamp/display concerns, NOT the reporting-window bug the flakes hit; keep g1 to the windowing fix. (3) **Two PRs:** g1 (IST `DayWindow` helper) first, then g2+g3. (4) barcode = Enter-to-add + refocus + unknown-barcode error only (no device/format/camera). ⚠️ `DayEndClose.BusinessDate` is a stored DATE — keep date-vs-date comparisons; only UTC-shift the `Sale.BillDate`-instant cash windows. *(minor — defaults; deferred stamping items flagged for owner)*

- **2026-07-02** — DESIGN (2(c) ledgers, architect-drafted, orchestrator-approved): **lightweight** (plan §3) — two thin entities `SupplierPayment` (money out) + `CustomerReceipt` (money in vs khata); statements **derived** by unioning existing purchases/purchase-returns (supplier) or credit-sales/sales-returns (customer) + payments/receipts + opening balance, running balance computed **in-memory** (SQLite grouped-decimal-SUM is brittle). `Customer.Balance` stays authoritative; **no `Supplier.Balance` column** — supplier payable fully derived (`OpeningBalance + ΣPurchase.Total − ΣPurchaseReturn − ΣPayment`). Migration `AddPartyLedger`; two services (`ICustomerLedgerService`/`ISupplierLedgerService`), `MasterResult<T>`, ACID + validated + audited via the domain record. *(minor — within plan §3)*
- **2026-07-02** — DECISION (2(c)): **over-payment/advance BLOCKED** for v1 — a receipt can't drive khata below 0; a supplier payment can't exceed derived payable (symmetric with returns' non-negative floor). Advances/credit balances = additive later. *(minor default)*
- **2026-07-02** — DECISION (2(c) RBAC): **reuse existing permissions** — receipt→`DoBilling` (Owner+Pharmacist+Cashier), supplier payment→`DoPurchases` (Owner+Pharmacist, not Cashier), view statement→`ViewReports`. No new permission ⇒ no §4 role-table change ⇒ no escalation. *(minor)*
- **2026-07-02** — ASSUMPTION (2(c) supplier side): every purchase treated as a **payable** (fully on-account); **no `Purchase.AmountPaid` flag added** (no schema change to a tested table). Cash-on-delivery = record the purchase then immediately record a matching `SupplierPayment`. **Flag for owner at Phase-2 review**; `Purchase.AmountPaid` is additive later if their workflow needs it. *(assumption — surface at review)*
- **2026-07-02** — DECISION (2(c)): customer statement shows **only khata-affecting rows** (credit sales + their returns + receipts) so closing reconciles to `Customer.Balance`; cash sales excluded. **Two dedicated nav modules** (Customer Ledger, Supplier Ledger), not Reports sub-tabs — cleaner write-action gating. *(minor)*
- **2026-07-02** — DECISION (2(c) review): **Cashier CAN reach the Customer-Ledger to record khata receipts** — the module was over-gated on `ViewReports` (Owner+Pharmacist) while the service + test grant receipt to `DoBilling` (incl. Cashier). Re-gate the CustomerLedger **module** on `DoBilling`; keep the **statement view + CSV/PDF export** gated on `ViewReports` inside the VM (Cashier records a receipt & sees the outstanding balance, not the full statement). Implements the prior receipt=DoBilling decision; **flag for owner confirmation at Phase-2 review** (cashiers handling khata collections). *(minor)*
- **2026-07-02** — FIX (2(c) review, touches a Phase-1 file): add a **future-`InvoiceDate` guard** to `PurchaseService.CreateAsync` (reject invoice date > today, mirroring the expiry-date check) — closes a post-dated-purchase anomaly where the derived supplier payable and the as-of-date statement diverge and a payment could be recorded against a not-yet-issued invoice. Small, tested, defensive. *(minor)*
- **2026-07-02** — DEFERRED (2(c) review): UTC-vs-local statement day-boundary is a **pre-existing codebase-wide convention** (`ReportService.NormalizeRange`), not a regression — fold the fix into **Phase-2(g)** (IST-aware report day boundaries) as a shared helper across ReportService + LedgerMath. Ledger-VM base-class de-duplication = known cleanup (defer). Double-materialisation in statement queries = won't-fix (matches the deliberate materialise-then-compute-in-memory convention). *(notes)*
- **2026-07-02** — DESIGN (2(d) GSTR-1 export, architect-drafted, orchestrator-approved): a **GSTR-1 report** as a thin composite over the existing GST/HSN pipeline — `IReportService.GetGstr1Async(year, month, placeOfSupply)` → `Gstr1Report` DTO (B2CS-by-rate · HSN Table-12 · Docs-Issued Table-13 · **Credit-Notes/returns-by-rate** · totals), reusing `GetHsnSummaryAsync` aggregation + `NormalizeRange`; reads **stored** per-line CGST/SGST so it reconciles exactly to what was billed. **Accountant-friendly multi-section CSV** + A4 PDF (`Gstr1Document`). Gated on existing `ViewReports` (no new permission). Month+year picker on the Reports hub. **B2B/B2CL genuinely N/A** — `Customer` has no GSTIN/state (retail-only, client #7). *(minor — plan §11 'GST/HSN summary for GSTR-1')*
- **2026-07-02** — DECISIONS (2(d)): (1) **Returns/credit notes:** B2CS + HSN reported **gross** (reconcile to existing sales/HSN reports) **PLUS a separate 'Credit Notes / Sales Returns' section by rate** (returns already carry CGST/SGST) → net = gross − returns; **flag owner** whether they prefer fully-netted B2CS. (2) **Place of supply** = pharmacy **state name** text (no numeric GST state code; `Customer` has no state) — add a `StateCode` setting later only if the accountant needs the 2-digit code. (3) **Docs Issued** section INCLUDED (first/last bill-no + count). (4) **Format** = clean multi-section CSV + A4 PDF (NOT the govt offline-utility byte format — additive later). (5) **UI** = month+year picker shown when GSTR-1 selected. No §4/plan change. *(minor — defaults; returns-handling + POS-code flagged for owner review)*
- **2026-07-02** — DESIGN (2(e) day-end + Cashier view, judge-panel of 3 architects → synthesis, orchestrator-approved): **derive-the-expected, persist-the-close.** Expected cash computed live from existing tables; one thin immutable **`DayEndClose`** row per store-day (UNIQUE `BusinessDate`) snapshots the un-derivable human inputs (OpeningFloat · CashSales/CashReceipts/CashRefunds/CashSupplierPayments breakdown · ExpectedCash · CountedCash · Variance · ClosingCarryForward · Note · ClosedAt · CreatedBy). Migration `AddDayEndClose`. `IDayEndService` (`GetDaySummaryAsync` · `CloseDayAsync` ACID+server-recompute · `GetCloseHistoryAsync`), `MasterResult<T>`, `DayEnd`-gated. New Day-End nav module (Cashier-reachable, unlike Reports). *(minor — plan §9 cash reconciliation · §6.1 Cashier day-end)*
- **2026-07-02** — DECISIONS (2(e)): (1) **Persist `DayEndClose`** (thin audit table, one per store-day). (2) **Expected cash** = OpeningFloat + Σ cash Sales + Σ cash CustomerReceipts − Σ cash SupplierPayments − Σ cash-bill SaleReturns; UPI/Card/Credit informational only. (3) **SaleReturn cash-out uses the parent-sale `PaymentMode` proxy** (SaleReturn has no PaymentMode; matches `CustomerLedgerService` khata logic) — **logged assumption, flag owner**; a real `SaleReturn.PaymentMode` is a follow-up (touches the 2(a) entity → sign-off). (4) **Opening float = carry-forward** from the prior close's ClosingCarryForward, editable. (5) **Whole-store close, Owner/Pharmacist-only**; **Cashier = read-only own-day view** (`CreatedBy`-scoped via `ISessionContext`, server-coerced) — **flag owner** to confirm cashiers do not close the till. (6) Saved close stores an **immutable snapshot** (back-dated txns never change a closed day). **RBAC reuses `DayEnd`** (Cashier+Owner+Pharmacist) — no new permission, no §4 change. *(minor — defaults; SaleReturn-cash-out proxy + cashier-cannot-close flagged for owner review)*
- **2026-07-02** — CLARIFICATION (2(e) review): **opening float is HONORED as an editable operator input** at close (`CloseDayAsync` uses `input.OpeningFloat`; VM live-recomputes expected/variance) — the impl had silently ignored it while showing it editable. Matches design decision #4 ('carry-forward with manual override'); safe because close is Owner/Pharmacist-only + fully audited in the immutable snapshot. Cash DELTAS stay server-recomputed (never trust UI). **Flag owner** at Phase-2 review (a locked/read-only float is a one-line change if they prefer stricter reconciliation). *(minor — aligns impl to intent)*
- **2026-07-02** — DESIGN (2(f) Schedule-X strict register + dual-Rx, architect-drafted, orchestrator-approved): keep the H1 gate untouched; add a separate **`ScheduleXDispense`** entity (FK `Sale`+`SaleItem`, denormalized product/batch/qty) capturing the dual-Rx field set only for Schedule-X lines (no null-bloat on `Sale`); enforce capture in `BillingService.CreateSaleAsync` (block the whole sale for any `DrugSchedule.X` line missing it, in the ACID tx). Migration `AddScheduleXDispense`. New **running-balance register** (DERIVED: per-substance opening + Σpurchase − Σsale ± returns = closing, + dispense detail) as a NEW report type complementing the existing combined H/H1/X log; CSV + A4 PDF; `ViewReports`-gated. *(minor — plan §14 Schedule-X compliance / owner Phase-2 scope)*
- **2026-07-02** — DECISIONS (2(f)): (1) **Capture fields** = patient name+address (+phone opt), prescriber name+address+**registration no**, prescription **number**+**date**, **`PrescriptionRetained`** attestation (must be true) — walk-in NOT allowed for X. (2) **Register = running-balance** (derived, no new stock-movement table) + dispense detail. (3) **RBAC: NO §4 change now** — Schedule-X capture is enforced for **all billers** (`DoBilling`); a **pharmacist-only-for-X** restriction (new `DispenseScheduleX` permission) is **NOT** added unilaterally (Rule #2 reserves §4 changes for owner sign-off) — **flagged for owner sign-off** at Phase-2 review. (4) Schedule-X membership relies on `Product.Schedule == X` in the catalog (data-entry responsibility). (5) Retention = documented only (app never deletes). **Exact legal field set + all above → owner/compliance-advisor confirmation at go-live.** *(minor — defaults; RBAC restriction + legal field set flagged for owner)*

---

## Client Answers — RESOLVED 2026-07-01 (plan §17)
*(the owner answered all 8 in-session; folded into plan.md; GitHub issue #2 to be updated/closed)*

1. **Concurrent billing PCs → ONE.** Stay on **SQLite** (offline, single file). ✓ confirms current design.
2. **Credit customers (khata) → YES, in v1.** Add a customer ledger (balance, part-payments, outstanding). `Customer` already has `credit_limit`/`balance`. **Confirmed in v1** (build in the billing phase).
3. **Printer → UNDECIDED.** Default: build the **3-inch thermal** GST receipt first; keep A4/A5 easy to add.
4. **Existing data → UNSURE.** Build the **CSV/Excel importer** anyway; confirm the source later.
5. **GST/HSN → prefill defaults now** (HSN 3003/3004, 12%/5%), CA reconciles exact values before go-live.
6. **Backup → LOCAL + CLOUD folder.** Automatic daily **encrypted** backup to a local folder AND a Drive/OneDrive-synced folder + one-click restore.
7. **License/sales → RETAIL ONLY.** Retail DL (Form 20/21) on the bill; no wholesale/B2B. (Supersedes the 20B/21B question.)
8. **Name → "Apex-Pharma"** (no rename; matches repo + code + header).
9. ✅ **Repo state:** confirmed **empty + private**; `gh` authenticated as Shuvrajit10101.

---

## Next Steps (ordered)

**Phase 2 (Round out) — ✅ COMPLETE & merged (PRs #16–#23).** `main` @ `a6595aa`, 424 tests, CI green. Next:
1. **→ Phase-2 OWNER REVIEW** — decisions I defaulted, awaiting sign-off: cashier khata-receipts (#20) · SaleReturn cash-out parent-sale proxy (#20) · editable opening-float at close (#20) · Schedule-X **pharmacist-only NOT added / capture enforced for all billers** + exact legal field set (#21) · GSTR-1 returns-as-separate-section + POS = state-name text (#19) · Pharmacist `ManageProducts` RBAC (Phase-1 open #3).
2. **Deferred IST-stamping follow-up (own PR, owner-confirm):** bill-number/expiry `UtcNow.Date` UTC-midnight rollover + invoice-date IST display + `StockAdjustment` history window (distinct from the shipped day-window fix).
3. **Phase 3** (plan §15, on owner priority): encrypted cloud backup/sync · owner KPI dashboard · refill/expiry reminders · GST-return e-file · multi-branch groundwork.
4. **Go-live checklist:** restore drill (`docs/go-live-restore-drill.md`) on the counter PC · owner sets the cloud backup folder · CA reconciles HSN/GST + Schedule-X legal fields · staff training + parallel run.
- Still open (non-blocking): deprecate the legacy batch-level purchase-return overload.

---

## Archive
*(older session detail moved here to keep the top lean — none yet)*
